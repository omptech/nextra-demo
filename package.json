{
    "dependencies": {
        "next": "^14.1.0",
        "nextra": "^2.13.3",
        "nextra-theme-docs": "^2.13.3",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "export": "NODE_ENV=production next build && touch out/.nojekyll",
        "lint": "next lint --max-warnings=0",
        "type-check": "tsc",
        "deploy": "gh-pages -d out -t true",
        "prepare": "husky install",
        "deploy": "gh-pages -d out",
        "cleanup-pr-preview": "node cleanup-pr-preview.js"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1"
    }
}
